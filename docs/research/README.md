# docs/research/

Investigations, library evaluations, link dumps, and recon notes that
inform a future plan but aren't a plan themselves. Name files
`<YYYY-MM-DD>-<short-kebab-topic>.md` so the directory sorts
chronologically. Anything that turns into an architectural decision
graduates from here into `docs/DESIGN.md`.
